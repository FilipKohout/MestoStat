FROM python:3.12-slim
WORKDIR /app

ARG DB_HOST
ARG DB_PORT
ARG DB_NAME
ARG DB_USER
ARG DB_PASS

ENV DB_HOST=$DB_HOST
ENV DB_PORT=$DB_PORT
ENV DB_NAME=$DB_NAME
ENV DB_USER=$DB_USER
ENV DB_PASS=$DB_PASS

COPY ./ ./

RUN pip install -r dependencies.txt

CMD ["python", "src/main.py"]
